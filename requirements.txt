# Base runtime dependencies
numpy >= 1.24
scipy >= 1.10

# Install the local project in editable mode (no optional extras)
-e .

# Optional extras:
#   pip install -e .[nomad]       # PyNomad-backed MADS optimiser
#   pip install -e .[examples]    # plotting/reporting helpers
#   pip install -e .[dev]         # linting, typing, tests
